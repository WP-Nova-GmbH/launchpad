# Copy to terraform.tfvars and fill in. terraform.tfvars is gitignored:
# the Hetzner token is an account-wide credential and stays out of the repo.
hcloud_token = "..."
relay_domain = "relay.example.com"

# ssh_keys       = ["id_ed25519"]
# location       = "fsn1"
# server_type    = "cx32"
# source_git_url = "https://github.com/WP-Nova-GmbH/launchpad.git"
